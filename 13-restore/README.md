Git restore to discard something that you staged (or added using git add). 

Restore specific file: 
```bash 
git restore --staged file-name
```

Restore all files: 
```bash 
git restore --staged .
```